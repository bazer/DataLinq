### YamlMime:ApiPage
title: Enum IndexType
body:
- api1: Enum IndexType
  id: DataLinq_Attributes_IndexType
  src: https://github.com/bazer/DataLinq/blob/master/src/DataLinq.Core/Attributes/IndexAttribute.cs#L8
  metadata:
    uid: DataLinq.Attributes.IndexType
    commentId: T:DataLinq.Attributes.IndexType
- facts:
  - name: Namespace
    value:
      text: DataLinq.Attributes
      url: DataLinq.Attributes.html
  - name: Assembly
    value: DataLinq.Core.dll
- markdown: Represents the underlying data structure or algorithm used by the index.
- code: public enum IndexType
- h2: Fields
- parameters:
  - name: BRIN
    default: "9"
    description: >+
      Represents PostgreSQL's Block Range INdexes, suitable for large tables with a natural sort order.

  - name: BTREE
    default: "0"
    description: >+
      Standard B-tree based index common across MySQL, MSSQL, SQLite, and PostgreSQL.

  - name: CLUSTERED
    default: "4"
    description: >+
      Represents the clustered index in MSSQL, determining the physical order of data.

  - name: COLUMNSTORE
    default: "6"
    description: >+
      Represents MSSQL's columnstore index, storing data in a column-wise manner.

  - name: FULLTEXT
    default: "1"
    description: >+
      Represents full-text search capabilities across MySQL, MSSQL, and SQLite.

  - name: GIN
    default: "7"
    description: >+
      Represents PostgreSQL's general inverted index, used for arrays, full-text search, etc.

  - name: GIST
    default: "8"
    description: >+
      Represents PostgreSQL's generalized search tree.

  - name: HASH
    default: "2"
    description: >+
      Represents hash-based index common to MySQL and PostgreSQL.

  - name: RTREE
    default: "3"
    description: >+
      Represents spatial index in MySQL.

  - name: SPATIAL
    default: "5"
    description: >+
      Represents spatial indices across MySQL and MSSQL.

languageId: csharp
metadata:
  description: Represents the underlying data structure or algorithm used by the index.
