> [!WARNING]
> This document is roadmap execution evidence for the DataLinq 0.9 development line. It records unsafe or incomplete current behavior and must not be read as shipped product documentation.

# Mutation Lifecycle Expected-Failure And Ownership Matrix

**Status:** W1 before-state audit complete. The `ML-2` pre-execution exits, `TX-1A` reference-identity tracking, `TX-1B` successful-only private mutation authority, `TX-2A` confirmed-success finalization for DataLinq-owned transactions, and bounded `TX-4A` post-preflight mutation-pipeline failure exits are implemented as of 2026-07-12 on the bounded `ML-1` provenance substrate. The full `ML-1` exit, `TX-2B` known-committed publication recovery, `TX-3`, provider-call exceptions with an outcome-unknown commit, raw low-level escape closure, attached completion, full concurrency semantics, and the rest of `TX-4`/`TX-5` remain assigned to W3.

**Target release:** 0.9.

**Audited:** 2026-07-10.

## Purpose

This is the explicit owner matrix for the mutation-lifecycle cases that the first W1 characterization slice deliberately did not turn green. It now also records which exits became executable with `ML-2`. It separates three different facts that are easy to blur together:

1. behavior that is already safe and must remain green
2. behavior that happens to succeed today without the lifecycle state needed to make it trustworthy
3. behavior that is observably unsafe and must be represented as a future-behavior test until its owning workstream lands

The matrix covers cross-provider and cross-transaction reuse, post-commit reuse, rollback and disposal, deletion, primary-key mutation, failed writes, read-only transactions, and attached external transactions. Test IDs below are durable behavior identifiers; focused executable tests may use descriptive method names rather than repeating those IDs literally.

## Audit Boundary

The audit inspected the active runtime under `src/DataLinq`, the SQLite and MySQL/MariaDB transaction adapters, and the active TUnit projects under `src/DataLinq.Tests.Unit`, `src/DataLinq.Tests.Compliance`, and `src/DataLinq.Tests.MySql`.

The `E*` evidence index is intentionally a static W1 **before-state** snapshot; its line references and present-tense descriptions are not silently rewritten as implementation lands. The status matrix below is the evolving record. The parallel TX-0 provider-lifecycle characterization freezes commit/rollback/disposal exception ordering under explicitly named `CurrentBehavior` tests; those tests document a gap rather than define the target contract. Mutation command-count and poison-state assertions land with their owning implementation slice so they require the accepted 0.9 behavior from their first committed form.

Status terms used below:

- **Green control:** the user-visible happy path is already covered, but its lifecycle implementation still needs to move under explicit provenance.
- **Green:** the required guard/transition has executable implementation evidence on its stated matrix.
- **Amber:** part of the desired result occurs, but timing, ownership, failure handling, or diagnostics are not trustworthy.
- **Red:** the current path has no required guard or terminal transition and can reach provider SQL or publish invalid state.

## W1 Before-State Evidence Index

| Evidence | Exact current location | What it proves |
| --- | --- | --- |
| E1 | `src/DataLinq/Instances/Mutable.cs:20-30` | A mutable stores an immutable row, `isNew`, `isDeleted`, and changed row data. There is no provider owner, transaction owner, invalid state, or invalidation reason. |
| E2 | `src/DataLinq/Instances/Mutable.cs:130-163` | Construction and `Reset(T)` copy row data and metadata but discard the immutable's data source. A successful statement therefore resets values without preserving the provider/transaction provenance of the hydrated row. |
| E3 | `src/DataLinq/Instances/Immutable.cs:128-133` | The source immutable does know its `IDataSourceAccess` and can move from a completed transaction to read-only access. The provenance is available before `Mutable(T)` discards it. |
| E4 | `src/DataLinq/Instances/Mutable.cs:37-66`, `src/DataLinq/Instances/Mutable.cs:86-113` | Primary-key setters are accepted and merely invalidate the cached key; the next `PrimaryKeys()` call adopts the assigned key. |
| E5 | `src/DataLinq/Instances/Mutable.cs:143-165` | Public reset changes the row baseline without lifecycle validation, while deletion is one irreversible Boolean set by `SetDeleted()`. |
| E6 | `src/DataLinq/Mutation/Transaction.cs:150-166`, `src/DataLinq/Mutation/Transaction.cs:187-207` | Insert/update validate only new-versus-existing shape, execute immediately, hydrate from the target provider's cache, and call `model.Reset(immutable)` before provider commit. |
| E7 | `src/DataLinq/Mutation/Transaction.cs:230-273`, `src/DataLinq/Database.cs:177-240` | `Save()` selects insert/update from `IsNew()`, and implicit database writes create a fresh transaction. Neither path checks mutable ownership. |
| E8 | `src/DataLinq/Mutation/Transaction.cs:279-290`, `src/DataLinq/Mutation/StateChange.cs:66-73` | Mutable delete sets `isDeleted` immediately after the statement, before commit. A later `StateChange` rejects the Boolean, but there is no transaction-local delete or invalid-delete state. |
| E9 | `src/DataLinq/Mutation/Transaction.cs:324-339` | A candidate is appended to public `Changes` before command execution. There is no catch, removal, or transaction poisoning if execution or transaction-local cache application throws. |
| E10 | `src/DataLinq/Mutation/StateChange.cs:112-164` | Mutation exceptions are recorded in telemetry and rethrown. The transaction and mutable are not transitioned to failed/invalid states. |
| E11 | `src/DataLinq/Mutation/StateChange.cs:207-223` | Update uses the mutable's current primary-key value both in `WHERE` and in the generated `SET` list; delete also uses the current value. It does not use the captured original primary key as the row selector. |
| E12 | `src/DataLinq/Mutation/Transaction.cs:344-352` | Provider commit precedes global cache publication, which is good. Publication and transaction-cache removal are sequential and have no failure partition or mutable promotion/invalidation. |
| E13 | `src/DataLinq/Mutation/Transaction.cs:357-392` | Rollback removes cache state only after provider rollback succeeds; dispose removes cache state before provider disposal. Neither path tracks or invalidates touched mutables. |
| E14 | `src/DataLinq/Mutation/Transaction.cs:373-383` | The only central validity guard explicitly returns for `TransactionType.ReadOnly`; otherwise it checks only committed/rolled-back provider status. |
| E15 | `src/DataLinq/Cache/TableCache.RowLookup.cs:219-229`, `src/DataLinq/Cache/TableCache.Invalidation.cs:25-34` | Read-only transactions do not receive transaction row caches, even though mutation application still treats every non-null transaction as transaction-local. Permitting writes here produces an internally inconsistent cache path. |
| E16 | `src/DataLinq/Mutation/Transaction.cs:134-142`, `src/DataLinq/Mutation/Transaction.cs:344-352` | An attached provider transaction uses the same wrapper commit/publication path as an owned transaction, but the wrapper records no ownership mode or external-completion observation. |
| E17 | `src/DataLinq.SQLite/SQLiteDatabaseTransaction.cs:139-153`, `src/DataLinq.MySql/Shared/SqlDatabaseTransaction.cs:130-144` | Attached adapters skip the underlying commit when its connection is no longer open, then still set DataLinq status to `Committed`. The outer wrapper can consequently publish `Changes` after an external commit or rollback without knowing which occurred. |
| E18 | `src/DataLinq.SQLite/SQLiteDatabaseTransaction.cs:161-203`, `src/DataLinq.MySql/Shared/SqlDatabaseTransaction.cs:152-193` | Provider rollback/disposal closes open attached or owned transactions, but the provider layer has no touched-mutable registry to invalidate. |
| E19 | `src/DataLinq/Mutation/StateChange.cs:52-83` | `StateChange` captures the current key and changes after only view/type/new/deleted checks. There is no provider, transaction, read-only, invalid-baseline, or primary-key-change preflight. |

## Existing Test Evidence And Its Limits

| Current test evidence | Exact location | What is green | What is not proved |
| --- | --- | --- | --- |
| Repeated implicit save and explicit same-transaction/post-commit save | `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionLifecycleTests.cs:570-638` | Earlier values survive repeated `Save()` calls; the same mutable can be used again after commit against the same database. | By itself this legacy test does not observe the touched registry, explicit promotion timing, ownership-token outcome, wrapper event ordering, rollback invalidation, or failure partitions. `TX-2A` adds separate direct evidence for the successful owned-commit transition. |
| Explicit rollback restores committed database values | `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionLifecycleTests.cs:293-326` | The provider rollback restores the database row. | Reusing the touched mutable is never attempted, so invalidation is not covered. |
| Rollback cache preservation and transaction-row cleanup | `src/DataLinq.Tests.Compliance/State/EmployeesCacheInvalidationCharacterizationTests.cs:197-240` | Committed row identity remains cached and transaction rows disappear. | The touched mutable is not written again after rollback. |
| Open transaction disposal cleanup | `src/DataLinq.Tests.Compliance/State/EmployeesCacheInvalidationCharacterizationTests.cs:242-284` | Provider disposal rolls back and transaction rows disappear. | The touched mutable is not invalidated or tested after disposal. |
| Committed delete cache invalidation | `src/DataLinq.Tests.Compliance/State/EmployeesCacheInvalidationCharacterizationTests.cs:64-91` | A committed delete removes the cached immutable row. | The test deletes an immutable, not a mutable whose lifecycle can become transaction-local, deleted, or invalid. |
| Attached write followed by external commit and wrapper commit | `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionLifecycleTests.cs:61-96` | The current external-first sequence happens to persist and lets the wrapper publish. | This is the sequence 0.9 explicitly makes unsupported after DataLinq touches a mutable. It does not prove wrapper-owned commit, rollback invalidation, or external-close detection. |
| Deterministic provider commit/rollback/disposal exceptions | `src/DataLinq.Tests.Unit/Core/TransactionFaultInjectionCharacterizationTests.cs:15-152` | Exact current exception propagation, status, and transaction-cache ordering are frozen for nine terminal-path partitions. In particular, commit/rollback failures retain cache state while direct wrapper-`Dispose` faults occur after outer cache cleanup. | The fixture deliberately has no touched mutable, mutation statement candidate, poisoned state, or committed-cache publication fault. Tests named `CurrentBehavior` are evidence for `TX-3`/`TX-4`, not accepted 0.9 outcomes. |
| `TX-1A` reference-identity touched tracking | `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionTrackingTests.cs` | Successful insert, update, and mutable delete register their exact lifecycle-aware mutable after the authoritative transaction-local transition. Repeated use of one object deduplicates, two equal-primary-key objects remain distinct, insert-then-update crosses the new-to-persisted lifecycle transition, a clean no-change update adds no entry while retaining an earlier touch, and immutable delete records its change without adding a mutable entry. | `TX-2A` now consumes the registry for confirmed-success owned commit promotion. `TX-2B`, `TX-3`, and the remaining `TX-4` still need terminal failure consumption; legacy custom mutables have no lifecycle contract to register. |
| `TX-1B` / bounded `TX-4A` mutation authority and poison gates | `src/DataLinq.Tests.Unit/Core/TransactionMutationFailureTests.cs`; `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionFailureTests.cs` | Only fully finalized mutations enter private ordered commit authority; public `Changes` mutation cannot alter it; public `StateChange` execution uses the same authority. Core faults cover command construction, provider execution, generated-ID decode, pending-cache application, authoritative reload, earlier-success retention, poison gates, legal disposal, captured assignment/in-place-array drift, callback mutation drift, frozen relation/index impact keys, mutation-cache callback managed reentrancy, null-FK/cache-hit read gates, terminal diagnostics, and the explicit raw-access bypass. The active provider matrix proves duplicate-key rollback/no-publication, public auto-increment `StateChange` hydration/commit, frozen indexed impact keys, and the null-FK poison gate. | Provider-call outcome uncertainty, `TX-2B` known-committed publication recovery, ordinary rollback/open-disposal invalidation, raw-handle closure, attached completion, convenience-helper cleanup exception partitioning, and full concurrency semantics remain open. `StateChange` is not a provider-neutral immutable receipt. |
| `TX-2A` confirmed-success owned finalization | `src/DataLinq.Tests.Unit/Core/MutableLifecycleTests.cs`; `src/DataLinq.Tests.Unit/Core/TransactionMutationFailureTests.cs`; `src/DataLinq.Tests.Compliance/Transactions/EmployeesTransactionCommitPublicationTests.cs` | After a successful owned provider commit, private changes publish globally, transaction rows disappear, touched mutables are promoted explicitly, committed deletion is preserved, the ownership token is committed, and the touched registry is cleared before the wrapper `Committed` event. A provider-committed transaction-bound immutable cannot switch read source during global notification, but can do so from the deferred wrapper callback. A throwing wrapper observer surfaces only after finalization. The provider fixture then reuses the promoted mutable successfully. | `TX-2B` publication/local-cleanup faults, a throwing provider `Commit()` call, attached completion, raw handles, and full concurrency remain open. The provider status itself becomes terminal before local finalization; the guard and deferred wrapper event close only the managed confirmed-success observation window. |

The W1 search found no active lifecycle tests for cross-provider reuse, cross-transaction rejection, primary-key mutation rejection, mutation-statement poisoning, read-only write rejection, mutable deletion terminal states, attached wrapper rollback, or external rollback/closure detection. `TransactionMutationGuardTests` and `EmployeesMutationGuardTests` now cover the `ML-2` subset, including zero command construction, exact owner rejection, primary-key/metadata integrity, read-only writes, callback ordering, and public `StateChange` backstops. `EmployeesTransactionTrackingTests` covers `TX-1A`; `TransactionMutationFailureTests` and `EmployeesTransactionFailureTests` cover `TX-1B` plus bounded `TX-4A`; `MutableLifecycleTests`, the commit-finalization core cases in `TransactionMutationFailureTests`, and `EmployeesTransactionCommitPublicationTests` cover `TX-2A`. `TX-2B` known-committed publication recovery, ordinary rollback/open-disposal invalidation, provider-call outcome uncertainty, raw low-level escape closure, attached/external completion, convenience-helper cleanup exception partitioning, and full concurrency semantics remain deliberately red until their owning slices land.

## Expected-Failure And Owner Matrix

### Provenance And Reuse

| Status | Proposed test ID | Entry condition | Required 0.9 exit condition | Current behavior and evidence | Owner | Evidence or remaining gap |
| --- | --- | --- | --- | --- | --- | --- |
| **Green** | `ML2-XP-01` | Materialize an immutable through provider instance `P1`, create and dirty mutable `M`, then call `Update`/`Save` through distinct provider instance `P2` with a compatible model shape. | Throw a DataLinq-owned cross-provider diagnostic before command creation/execution. `P2.Changes` and both provider caches remain unchanged; `M` retains its assignments and its `P1` committed baseline. | Mutable provenance retains the exact provider instance and preflight uses reference identity before both ordinary and no-change paths. `EmployeesMutationGuardTests.CrossProviderGuards_*` verifies `Update`, `Save`, and immutable `Delete` against isolated provider stores; the core probe proves zero writer/command/execution calls. | `ML-1` provenance, `ML-2` guard, provider matrix `TX-6` | Green on the active SQLite/MySQL/MariaDB compliance matrix once the final broad run is recorded; no touched registry is required for rejection before the first successful write. |
| **Green** | `ML2-XT-01` | `M` has completed a successful insert/update in still-open transaction `T1`; dirty it again and pass it to distinct explicit transaction `T2` on the same provider. | Reject before command creation. `M` remains transaction-local to `T1`; `T2.Changes` stays empty; neither transaction cache changes. `T1` may still commit or roll back normally. | Hydration binds an opaque exact token and preflight accepts only that same token. Dirty and clean/no-change `T2` attempts reject without a cache scope; `T1` then reuses the mutable successfully in `EmployeesMutationGuardTests.CrossTransactionGuards_*`. | `ML-1`, `ML-2`; provider matrix `TX-6` | Green for pre-execution ownership. `TX-1A` enumerates every successfully transitioned lifecycle mutable and `TX-2A` consumes that registry on confirmed owned commit; `TX-2B`, `TX-3`, and the remaining `TX-4` failure exits remain open. |
| **Green** | `ML2-XT-02` | `M` is transaction-local to open explicit `T1`; call an implicit `Database.Save(M)`/`Update(M)` on the same provider. | Reject before the implicit transaction creates or executes a command. `M` remains owned by `T1`; the short-lived transaction publishes nothing. | Implicit writes create a distinct token and therefore fail the same exact-token guard. Both deterministic core and provider compliance tests preserve the original token/assignments and prove the owning transaction remains usable. | `ML-2`; provider matrix `TX-6` | Green; convenience APIs no longer escape unresolved explicit ownership. |
| **Green** | `TX2-PC-01` | Successfully write `M` in DataLinq-owned explicit transaction `T`, commit `T` through DataLinq, then dirty and save `M` through the same provider. | Commit promotes `M` from transaction-local to clean committed state only after provider commit, committed-cache publication, and transaction-cache cleanup. Later same-provider save succeeds from that promoted baseline. | `TX-2A` explicitly promotes the reference-identity touched registry, marks the exact ownership token committed, and clears the registry after global/local cache finalization. `EmployeesTransactionCommitPublicationTests` observes the promoted state from the deferred callback and then persists a later reuse through the same provider. | `TX-2A`, provider regression `TX-6` | Green for confirmed-success owned commit. `TX-2B` publication/local-cleanup failure recovery, provider-call outcome uncertainty, and attached completion remain red/amber. |
| **Green** | `TX2-FINALIZE-01` | Commit an owned transaction while a global cache subscriber and wrapper `Committed` observer inspect transaction-bound state; repeat with a throwing wrapper observer. | Global notification runs after provider commit but cannot switch transaction-bound objects to committed reads before local finalization. The wrapper event runs only after publication, transaction-cache removal, touched promotion, token commit, and registry clearing; an observer exception cannot interrupt those steps. | Core tests observe the guarded provider-committed window, the fully finalized deferred callback, and throwing-observer ordering. Provider compliance observes committed global notification, outside committed reads, empty local state/registry, committed token, and promoted mutable from the callback. | `TX-2A`, core and provider regression `TX-6` | Green for the managed confirmed-success observation window. This is not a full concurrency guarantee and does not recover `TX2-PUBLISH-01`. |

### Rollback, Disposal, And Delete

| Status | Proposed test ID | Entry condition | Required 0.9 exit condition | Current behavior and evidence | Owner | Evidence or remaining gap |
| --- | --- | --- | --- | --- | --- | --- |
| **Red** | `TX3-RB-01-I/U/D` | Successfully insert, update, or mutable-delete through explicit transaction `T`, then call `T.Rollback()`. Attempt a later implicit and explicit write with each touched mutable. | Provider rollback occurs; all transaction rows/notifications are removed; every touched mutable becomes invalid with `RolledBack`; later writes reject before SQL and direct the caller to materialize a fresh committed row. Committed cache state remains intact. | Provider/database and cache rollback are covered, but mutable reuse is not. `TX-1A` now registers successfully transitioned lifecycle mutables, but runtime rollback only invokes the provider and removes transaction cache state without consuming that registry (E13); insert/update mutables retain statement-time transaction values (E6), while delete retains its transaction-local deleted state (E8). | `TX-3`, with provenance from `ML-1` and registry from `TX-1`; provider matrix `TX-6` | Current values can look clean even though they were never committed. Update/insert mutables have no invalid state and may be reused. Delete rejects for the wrong terminal reason and cannot explain rollback. |
| **Red** | `TX3-DISP-01-I/U/D` | Successfully touch mutable(s) in open transaction `T`, then dispose `T` without commit or explicit rollback; repeat with a provider-dispose fault in the core fake lane. | Treat open disposal as rollback: invalidate all touched mutables with `OpenTransactionDisposed`, remove local cache state in a finally-safe path, and retain the provider exception if disposal fails. Later writes reject before SQL. | Provider disposal rolls back an open transaction (E18), and current cache cleanup is covered. The outer wrapper now has the `TX-1A` registry after successful transitions and removes cache state before provider disposal, but disposal does not consume that registry for invalidation (E13). | `TX-3`, core fault test and provider matrix `TX-6` | Current tests stop after database/cache assertions. No mutable terminal transition exists, and provider-dispose failure cannot be tied to mutable invalidation. |
| **Green** | `TX2-DEL-01` | Pass an existing mutable `M` to `Delete()` in DataLinq-owned transaction `T`, observe it before commit, commit through DataLinq, then attempt `Insert`, `Update`, `Save`, and another `Delete` with `M`. | After the statement, represent a transaction-local delete owned by `T`; after successful commit, promote it to a `Deleted` terminal state owned by the provider. Every later mutation rejects before SQL with an actionable deleted-row diagnostic. | Mutable delete establishes transaction-local deleted provenance. `TX-2A` explicitly promotes that registry entry after publication/local cleanup while preserving the deleted row kind, commits the token, and clears the registry; `ML-2` rejects later mutation before command construction. Focused lifecycle and final callback tests observe the transition. | `TX-1` delete state, `TX-2A` promotion, `ML-2` guard; provider matrix `TX-6` | Green for confirmed-success owned commit. Rollback, open disposal, provider-call outcome uncertainty, `TX-2B` known-committed local failure, and attached completion remain amber/red. |
| **Amber/red semantics** | `TX3-DEL-02` | Mutable `M` is successfully deleted in open `T`; roll back or dispose `T`; then attempt to reset and write `M`. | `M` is invalid with the rollback/disposal reason, not restored heuristically and not labeled as a committed delete. `Reset()` cannot resurrect it; every later write rejects before SQL. | Current `isDeleted` remains true because neither rollback nor reset clears or reclassifies it (E5/E8/E13). Later `StateChange` is likely rejected, but only as "deleted," which is factually wrong after the database delete rolled back. | `TX-3`, `ML-1`, `ML-2`; provider matrix `TX-6` | Rejection alone is insufficient: recovery guidance and outcome semantics are wrong, and no mutable-delete rollback test exists. |

### Preflight And Write Failures

| Status | Proposed test ID | Entry condition | Required 0.9 exit condition | Current behavior and evidence | Owner | Evidence or remaining gap |
| --- | --- | --- | --- | --- | --- | --- |
| **Green** | `ML2-PK-01` | Materialize existing row with primary key `K1`, assign ordinary mutable primary key `K2` (with and without another field change), and call `Update`/`Save`. Include a fixture where `K2` already identifies another row. | Reject before command creation/execution. The mutable remains dirty against its `K1` baseline, transaction `Changes` stays empty, and neither cache nor row changes. | Preflight rejects every tracked mapped PK assignment and independently compares current canonical provider identity with the authoritative baseline, covering in-place binary/reference-ID drift. Exact table/column identity prevents foreign metadata handles from laundering a same-named PK assignment. Public `StateChange` revalidates its captured candidate. Core zero-command tests and two-row provider tests cover key-only/key-plus-value collisions. | `ML-2`, provider matrix `TX-6` | Green; ordinary key migration remains deliberately unsupported and diagnostics direct callers to reset or explicit delete/insert. |
| **Green** | `TX4-FW-01` | In explicit transaction `T`, execute an insert/update/delete that deterministically fails at the provider statement (for example, a unique or foreign-key constraint). | The failed candidate never enters successful `Changes`; its mutable becomes invalid with `MutationFailed`; `T` becomes poisoned; reads, writes, and `Commit()` reject; only rollback/dispose are legal; nothing is globally published. | The transaction-owned execution path appends only after full finalization. A post-preflight statement failure installs the first failure record, invalidates the current lifecycle mutable, rethrows the exact provider exception, and gates later managed use. | `TX-1B` successful-change recording, bounded `TX-4A` poisoning, core fault lane plus provider matrix `TX-6` | Deterministic command-construction/provider faults and real duplicate-key failures are green across the active provider matrix. Raw `DatabaseAccess` and underlying transaction handles remain an explicit unmanaged bypass. |
| **Green** | `TX4-FW-02` | Execute one successful write in `T`, then a second write that fails. Attempt `T.Commit()`. | The first mutable and failed mutable are invalidated; commit is rejected before provider commit; rollback/dispose removes all pending state; the earlier successful statement is not committed or published. | The earlier success remains in private diagnostic authority, the failed candidate is excluded, both lifecycle mutables become invalid with `MutationFailed`, and managed `Commit()` rejects before provider commit. Rollback remains legal and removes pending database/cache state. | Bounded `TX-4A`, with `TX-1B`; core fault lane and provider matrix `TX-6` | Green in the deterministic earlier-success/failure test and the real duplicate-key rollback matrix; provider commit-unknown remains a separate red partition. |
| **Green** | `TX4-HYDRATE-01` | Make provider execution succeed, then fault generated-value hydration, row reload, transaction-local cache application, or lifecycle finalization before the statement is finalized. | No publishable successful change; transaction poisoned; touched/current mutable invalid; only rollback/dispose legal; no global cache publication. User assignments remain inspectable. | Generated-ID decode, cache-notification/application, callback mutation drift, authoritative reload, and lifecycle/recording stages execute inside one failure partition. Any fault excludes the candidate, preserves inspectable assignments, poisons the wrapper, and invalidates the current and prior touched lifecycle mutables. | `TX-1B`, bounded `TX-4A`; core fault-injection lane `TX-6` | Green for the implemented mutation pipeline. Provider commit unknown and post-commit publication remain separate open partitions. |
| **Red** | `TX4-COMMIT-01` | Successful pending writes exist; the provider `Commit()` call throws or has unknown outcome. Attempt another write or commit, then clean up. | Do not publish globally; invalidate all touched mutables with `CommitOutcomeUnknown`; reject further writes/commit; allow rollback where possible or dispose; require fresh reads. | `TX-2A` enters managed finalization and still preserves provider-first ordering, so global publication is skipped when the provider call throws. But the call is not classified as outcome-unknown, touched mutables/ownership remain unresolved, and no uncertain terminal state or finally-safe invalidation exists. | `TX-4`; core provider-lifecycle fault lane `TX-6` | A provider call that throws may still have committed. It is deliberately outside the confirmed-success `TX-2A` contract. |
| **Red** | `TX2-PUBLISH-01` | Provider `Commit()` returns successfully, then committed/global cache publication or transaction-cache cleanup throws. Attempt to read/reuse touched mutable(s). | Report that database commit succeeded; remove transaction-local state; conservatively clear affected committed cache state; invalidate touched mutables; never report rollback; require a fresh committed read. | `Commit()` calls global `ApplyChanges` and then removes transaction rows. If either local step throws, later touched promotion, token commit, registry clearing, and deferred wrapper `Committed` publication are skipped. No conservative committed-cache cleanup or known-committed diagnostic is guaranteed, and touched mutables remain transaction-local despite a committed database. | `TX-2B` publication partition, with `TX-4` diagnostics; core fault lane `TX-6` | This is a committed database with failed local finalization, not a normal commit and not a rollback. `TX-2A` proves only the all-successful path. |
| **Green** | `ML2-RO-01-I/U/S/D` | Open `TransactionType.ReadOnly` and call `Insert`, `Update`, `Save`, or `Delete`; also exercise the database convenience overloads with `transactionType: ReadOnly`. | Reject before `StateChange` construction and provider command execution. `Changes` and caches stay unchanged; mutable state is unchanged; the read-only transaction remains usable for reads and normal terminal completion. | Mutation preflight now rejects read-only wrappers instead of returning. Core direct/convenience routes prove zero writer/command/execution calls; provider compliance proves the rejected wrapper remains readable and can commit normally, with no persisted mutation. | `ML-2`, core command-count assertion and provider matrix `TX-6` | Green. |

### Attached External Transactions

| Status | Proposed test ID | Entry condition | Required 0.9 exit condition | Current behavior and evidence | Owner | Evidence or remaining gap |
| --- | --- | --- | --- | --- | --- | --- |
| **Amber** | `TX5-ATTACH-COMMIT-01` | Begin provider `IDbTransaction`, attach it, perform DataLinq write(s), and call only the DataLinq wrapper's `Commit()`. Reuse the mutable through the same provider afterward. | Wrapper commits the provider transaction, publishes committed state, removes local rows, and promotes touched mutables exactly like an owned transaction. | The attached wrapper currently shares the `TX-2A` finalization code and would consume the touched registry after its provider adapter returns from commit. However, the wrapper records no ownership mode/external-completion observation, attached adapters can report committed after an already externally closed transaction (E16/E17), and the current provider test commits externally first rather than proving the supported wrapper-owned sequence. | `TX-5`, using `TX-1`/`TX-2A`; provider matrix `TX-6` | Shared code is not attached-completion evidence. The supported sequence and mutable promotion remain unproved, while ambiguous external completion remains unsafe. |
| **Red** | `TX5-ATTACH-RB-01` | Attach an open provider transaction, perform DataLinq writes, and call only wrapper `Rollback()`. | Provider rollback, local-cache cleanup, and invalidation of every touched mutable with `RolledBack`; nothing globally published. | Attached rollback uses the generic rollback path (E13/E18), which now has `TX-1A` tracking but does not consume it for invalidation. There is no focused attached-write rollback test. | `TX-5`, using `TX-3`; provider matrix `TX-6` | Database rollback may work, but the lifecycle contract does not exist. |
| **Red** | `TX5-ATTACH-EXTERNAL-01-C/R` | Attach and perform at least one DataLinq mutation, then commit or roll back the original `IDbTransaction` directly. Invoke the next wrapper read/write/commit operation. | Detect external closure where possible; poison/invalidate rather than infer commit versus rollback; perform no guessed global publication; emit a deterministic unsupported-completion diagnostic. | Provider adapters can see the connection is no longer open, skip the underlying commit, and still mark themselves committed (E17). Wrapper `Commit()` can then apply its private successful authority globally and run `TX-2A` finalization without knowing whether the external action committed or rolled back. After external rollback, this can publish changes that do not exist in the database. The existing external-commit-first test approves only the lucky half of this ambiguity. | `TX-5`, failure handling from `TX-4`; core and provider matrix `TX-6` | Current status transition cannot distinguish external commit from external rollback. Publishing in that state is factually unsafe. |
| **Red** | `TX5-ATTACH-DISP-01` | Attach, perform DataLinq writes, then dispose the wrapper without an observed wrapper commit; include already externally closed and provider-dispose-fault variants. | Conservatively invalidate touched mutables, remove local cache state, and roll back/dispose the still-open provider transaction where possible. Never infer that a mutable became committed. | Disposal removes cache rows and delegates provider rollback/disposal (E13/E18), and `TX-1A` knows lifecycle mutables touched by successful transitions, but disposal does not consume that registry for invalidation. If the original transaction was externally committed, the wrapper cannot reconstruct publication or safely promote. | `TX-5`, `TX-3`, `TX-4`; core fault lane and provider matrix `TX-6` | Cache cleanup alone cannot make mutable baselines trustworthy, and external completion remains ambiguous. |

## Test Entry/Exit Invariants

Every implementation test above should make the command and cache boundary observable, not merely assert that some exception was thrown.

### Preflight exits

For `ML2-XP-*`, `ML2-XT-*`, `ML2-PK-*`, and `ML2-RO-*`:

- provider mutation command count remains zero
- transaction `Changes` remains empty
- no transaction row cache or notification is created by the rejected operation
- the mutable's prior provenance and user assignments remain unchanged
- the transaction remains usable when the rejection is a caller preflight error rather than a provider failure

### Successful statement, pre-terminal exits

For same-transaction reuse and attached/owned success:

- the provider statement has succeeded
- the change is present exactly once in the successful collection
- the hydrated mutable is clean and transaction-local to the exact owning wrapper
- only that transaction sees its row/cache/notification effects
- another transaction or implicit write cannot consume the baseline

### Failure exits

For `TX4-*` and provider lifecycle faults:

- a failed candidate is absent from successful `Changes`
- no later `Commit()` can globally publish either the failed candidate or earlier pending successes from the poisoned transaction
- all touched mutable baselines are invalid even if provider rollback/disposal cleanup throws
- implemented bounded diagnostics distinguish statement, generated/authoritative hydration, transaction-local cache, and lifecycle-finalization failure; unknown commit outcome and committed-database/local-publication diagnostics remain future evidence
- user assignments may remain inspectable, but public `Reset()` cannot make the mutable writable again

### Terminal exits

For commit, rollback, open disposal, and mutable delete:

- provider completion occurs before the corresponding mutable promotion or invalidation
- transaction cache entries disappear on every terminal path
- confirmed-success owned commit promotes non-deleted mutables, leaves deleted mutables terminal, commits the ownership token, clears the registry, and only then publishes the wrapper `Committed` event
- rollback/open disposal invalidates all touched mutables rather than reconstructing a guessed committed baseline
- committed cache and relation state changes only after confirmed provider commit

## Implementation Order Implied By The Matrix

The matrix cannot be implemented safely as a pile of isolated exception checks. The dependency order is substantive:

1. `ML-1` adds provider/transaction provenance and invalid reasons without changing public equality or property APIs.
2. `ML-2` installs command-free preflight for cross-provider, cross-transaction, invalid/deleted, primary-key, and read-only cases.
3. `TX-1` records only successful changes and tracks touched mutables by reference identity.
4. `TX-2A` promotes and finalizes managed status observation after confirmed owned commit/publication; `TX-2B` separately recovers known-committed publication/local-cleanup failure.
5. `TX-3` invalidates in finally-safe rollback/open-disposal cleanup.
6. bounded `TX-4A` poisons statement/hydration/local-cache/lifecycle failures and narrows legal recovery to rollback/dispose; the remaining `TX-4` partition handles provider-call exceptions with an unknown commit outcome.
7. `TX-5` applies those same transitions to attached transactions and rejects ambiguous external completion.
8. `TX-6` runs the accepted cases across SQLite, MySQL, and MariaDB after the deterministic core fault lane is green.

Skipping directly to provider compliance tests would make results depend on locks, connector error policy, and cache accidents. The preflight and fault partitions belong in deterministic core tests first; provider tests then prove that the contract survives real connectors.

## W1 Exit Statement

At the W1 exit, linking this inventory did **not** make any red or amber row green; it only assigned implementation and executable acceptance to W3. That historical rule still matters because a prose reclassification without code/tests is not progress.

Since W1, the bounded provenance substrate and `ML-2` implementation have made the explicitly marked pre-execution rows green with deterministic command-count and provider-matrix evidence. `TX-1A` supplies the reference-identity registry, `TX-1B` records only fully finalized changes in private commit authority, `TX-2A` consumes both on the confirmed-success owned path, and bounded `TX-4A` closes the mutation-pipeline failure rows above. `TX-2B`, `TX-3`, provider-call outcome uncertainty, raw-handle, and `TX-5` rows stay amber/red until their failure-transition and attached-completion slices consume that authority; successful owned finalization and mutation poisoning do not launder those later gaps.
